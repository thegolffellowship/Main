# Nixpacks build configuration for Railway
# No special system packages or build commands needed — Golf Genius sync
# uses HTTP requests (no browser/Playwright required).
#
# 2026-09-18: adding [phases.setup] aptPkgs (Pango/Cairo for WeasyPrint, the
# print-pack PDF engine) FAILED the Railway build. Backed out in v2.465.1 so
# main deploys; the print pack degrades to a 503 until the engine ships —
# see docs/claude/events.md "print pack" for the retry plan.
