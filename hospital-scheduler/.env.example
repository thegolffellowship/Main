# Database URL - set to PostgreSQL for cloud deployment
# Leave unset to use local SQLite
# DATABASE_URL=postgresql://user:password@host:5432/hospital_scheduler

# Port (set automatically by most cloud platforms)
# PORT=8000
