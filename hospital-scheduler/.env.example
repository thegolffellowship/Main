# Database URL - auto-set by Railway when you add a PostgreSQL plugin
# Leave unset to use local SQLite for development
# DATABASE_URL=postgresql://user:password@host:5432/hospital_scheduler

# Port (set automatically by Railway)
# PORT=8000
